import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdatetypeuserPage } from './updatetypeuser.page';

describe('UpdatetypeuserPage', () => {
  let component: UpdatetypeuserPage;
  let fixture: ComponentFixture<UpdatetypeuserPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(UpdatetypeuserPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
